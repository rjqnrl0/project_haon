[project]
name = "v-suitcase-backend"
version = "0.1.0"
description = "V-Suitcase AI Virtual Fitting Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
